Card from @evidura/ui. Use via `window.EviduraUI.Card` (bundle loaded from the root `_ds_bundle.js`).

Surface container — radius md, soft ink shadow, `bg-2`/border chrome.

## Props

```ts
interface CardProps {
  /** Adds a hover-elevation transition (for clickable cards). */
  interactive?: boolean;
  className?: string;
  id?: string;
  style?: React.CSSProperties;
  children?: React.ReactNode;
}
```

## Related

`CardBody`, `CardHeader`, `CardTitle`
