DFVA_MOCK=true
OPENAI_API_KEY=***
STRIPE_API_KEY=***
LEMONSQUEEZY_API_KEY=***
POLAR_ACCESS_TOKEN=***
AWS_S3_REGION=us-east-1
AWS_S3_IAM_ACCESS_KEY=***
AWS_S3_IAM_SECRET_KEY=***
AWS_S3_FILES_BUCKET=***
# Local Postgres runs via Apple `container` (scripts/dev-db.sh start), not `wasp start db`/Docker.
# Setting DATABASE_URL puts Wasp in custom-db mode (intended): run `wasp start`, not `wasp start db`.
DATABASE_URL=postgresql://postgresWaspDevUser:postgresWaspDevPass@localhost:5432/OpenSaaS-fc3b171ec3
